# Copy this file to .env and fill in your own Supabase Postgres connection string.
# Free account: https://supabase.com -> New project -> Project Settings -> Database
# -> Connection string -> URI (use the "Connection pooling" string if offered).
DATABASE_URL=postgresql://postgres:<password>@<host>:5432/postgres

# Optional, defaults to 3000
PORT=3000
